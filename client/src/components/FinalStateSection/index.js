import React from 'react'
import {
    Box,
    Typography
} from '@material-ui/core'

const FinalStateSection = (props) => {
    const {finalState} = props


    return (
        <Box>
            <Typography variant='h6' align='center'>
                Heard Back?
            </Typography>
            
        </Box>
    )
}